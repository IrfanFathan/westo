import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF1B5E20); // Dark Green
  static const Color secondary = Color(0xFF00796B); // Teal
  static const Color accent = Color(0xFFFFB300); // Amber

  // Status Colors
  static const Color success = Color(0xFF2E7D32);
  static const Color warning = Color(0xFFF9A825);
  static const Color error = Color(0xFFD32F2F);

  // Background Colors
  static const Color background = Color(0xFFF4F6F8);
  static const Color surface = Color(0xFFFFFFFF);

  // Text Colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF616161);
  static const Color textDisabled = Color(0xFF9E9E9E);

  // Border & Divider
  static const Color border = Color(0xFFE0E0E0);

  // Connection Status
  static const Color connected = Color(0xFF388E3C);
  static const Color disconnected = Color(0xFFD32F2F);
}
