import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors — refined teal-based palette
  static const Color primary = Color(0xFF0D7377);       // Deep Teal
  static const Color primaryLight = Color(0xFF14A3A8);   // Light Teal
  static const Color primaryDark = Color(0xFF084C4F);    // Dark Teal
  static const Color secondary = Color(0xFF323F4B);      // Slate Grey
  static const Color accent = Color(0xFFFFB300);         // Amber

  // Gradient Colors
  static const Color gradientStart = Color(0xFF0D7377);
  static const Color gradientEnd = Color(0xFF14A3A8);

  // Status Colors
  static const Color success = Color(0xFF2E7D32);
  static const Color warning = Color(0xFFF9A825);
  static const Color error = Color(0xFFD32F2F);

  // Trigger Colors
  static const Color triggerEnabled = Color(0xFF2E7D32);
  static const Color triggerDisabled = Color(0xFF9E9E9E);

  // Background Colors
  static const Color background = Color(0xFFF0F4F8);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF5F7FA);

  // Text Colors
  static const Color textPrimary = Color(0xFF1F2933);
  static const Color textSecondary = Color(0xFF616E7C);
  static const Color textDisabled = Color(0xFF9AA5B1);

  // Border & Divider
  static const Color border = Color(0xFFE4E7EB);
  static const Color divider = Color(0xFFE4E7EB);

  // Connection Status
  static const Color connected = Color(0xFF2E7D32);
  static const Color disconnected = Color(0xFFD32F2F);
}
